import { render } from '@testing-library/react'
import Listadoimagenes from "../../components/Listadoimagenes";

test(' comprobar listado de imagenes' , () => {
    expect(Imagen (key:"imagen.id", imagen:"imagen").toBe(2)
)});
